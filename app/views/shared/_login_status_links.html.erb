<ul class="navbar-nav ml-auto">
  <li class = "nav-item">
    <%=link_to (t :about) , '#',  class: 'nav-link'%>
  </li>
  <!-- If User is Logged In, we show the Sign Out button -->
  <% if signed_in? %>

    <%= link_to "Log Out", destroy_user_session_path,
      method: :delete,
      class: 'btn btn-trunfo navbar-btn' %>
  <%end%>

</ul>